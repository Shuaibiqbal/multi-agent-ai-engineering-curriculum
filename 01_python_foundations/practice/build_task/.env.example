# practice/build_task/.env.example
# Why: shows every teammate which keys to set, without leaking real values into git.
# Copy this file to .env and fill in the real values. Never commit .env.
OPENAI_API_KEY=
LOG_LEVEL=INFO
